{
  "name": "rd-vision-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for RD Vision services and packages.",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r --parallel --filter ./services/* start:dev",
    "build": "pnpm -r --filter ./packages/* --filter ./services/* build",
    "start": "pnpm -r --filter ./services/* start",
    "clean": "pnpm -r run clean"
  },
  "keywords": [
    "rd-vision",
    "rd",
    "vision",
    "ai",
    "artificial-intelligence"
  ],
  "author": "Anderson Gonçalves <anderson.coder@gmail.com>",
  "license": "ISC",
  "packageManager": "pnpm@10.16.1",
  "private": "true",
  "devDependencies": {
    "@swc/cli": "^0.7.8",
    "@swc/core": "^1.13.5",
    "@swc/jest": "^0.2.39",
    "jest": "^30.1.3",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2"
  }
}
